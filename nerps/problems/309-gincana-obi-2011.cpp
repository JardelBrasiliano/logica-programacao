#include<bits/stdc++.h>
#include<vector>

using namespace std;

int main ()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);

	vector< pair<int, int> > v;
 	int n, m;cin >> n >> m;

	for(int i = 0; i < m; i++) {
    int inicio, fim;
    cin >> inicio >> fim;
    v.push_back( make_pair(inicio, fim) );
	}

	for(int i = 0; i < m; i++) {
		int atual = 
		cout << v[i].first;
	}

//   .first
// .second
	return 0;
}